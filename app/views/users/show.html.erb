<% provide(:title, @user.name) %>
<div class="container">
    <h1>
        <%= gravatar_for(@user) %>
        <%= @user.email %>
    </h1>
    <h3>Users URLses</h3>

    <%= render 'urls' %> 

</div>

